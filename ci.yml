name: CI/CD Pipeline

on:
  push:
    branches: [ "main", "develop" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2

    - name: Install Docker Compose
      run: |
        sudo curl -L "https://github.com/docker/compose/releases/download/v2.20.0/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
        sudo chmod +x /usr/local/bin/docker-compose

    # === اجرای اسکریپت جادویی ما ===
    - name: Start Environment & Setup DB
      run: ./init_project.sh

    - name: Run Tests (Admin Site)
      run: docker-compose -f docker-compose.admin.yml exec -T admin_site python manage.py test

    - name: Run Tests (Customer Site)
      run: docker-compose -f docker-compose.customer.yml exec -T customer_site python manage.py test
